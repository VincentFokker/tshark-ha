name: "PostgreSQL"
version: "1.0.2"
slug: "postgres"
description: "PostgreSQL relational database server with persistent storage"
url: "https://github.com/VincentFokker/tshark-ha"
arch:
  - amd64
  - aarch64
startup: system
init: false
boot: auto
host_network: true
options:
  username: "homeassistant"
  password: "changeme"
  database: "homeassistant"
schema:
  username: str
  password: str
  database: str
map:
  - data:rw
